{
  "name": "series-recommendation",
  "private": true,
  "version": "0.0.0",
  "description": "Root tooling (git hooks) for the TV Series Tracker monorepo. The actual app lives in backend/ and frontend/.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0"
  }
}
